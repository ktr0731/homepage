#!/bin/bash

cd ..
git config --global user.name "$GIT_USER_NAME"
git config --global user.email "$GIT_USER_EMAIL"
git clone "$DEST_REPO" dest
cp -rf ./homepage/dest/* dest/

cd dest || exit 1
git add .
git commit -m "Update with $(git rev-parse HEAD)"
git push origin master
